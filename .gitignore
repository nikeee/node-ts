# js files will only be relevant for the released npm package, so don't inclode them
*.js

# except the prepublish script
!bundle.js

# d.ts files can be generated, so don't include them
*.d.ts

# exclude VS-generated files/folders
bin/
obj/
*.user
*.js.map
*.suo

npm-debug.log

# nodejs tools dat files
*.dat

# exclude node modules, they will be installed using npm
node_modules/
